#!/bin/bash

export ARC_PLUGIN_PATH=${ARC_PLUGIN_PATH}
export CLASSPATH=${CLASSPATH}
export LD_LIBRARY_PATH=${LD_LIBRARY_PATH}

# Remove leading './' from first argument
class=${1#./}
# Remove trailing '.class'
class=${class%.class}
@JAVA@ junit.textui.TestRunner ${class}
