TESTSCRIPTS = test_bloomfilter.py

if ACIX_TESTS_ENABLED
TESTS_ENVIRONMENT = PYTHONPATH=$(top_srcdir)/src/services $(TRIAL)
TESTS = $(TESTSCRIPTS)
else
TESTS =
endif

check_SCRIPTS = $(TESTS)

EXTRA_DIST = $(TESTSCRIPTS)
