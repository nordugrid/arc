pkglibexec_SCRIPTS = janitor

# Do we really need this setuid wrapper?
rjanitor_SOURCES  = rjanitor.c
noinst_PROGRAMS = rjanitor

man_MANS = janitor.8

# Cleaning all files created by autoconf
CLEANFILES = $(dist_bin_SCRIPTS) $(noinst_PROGRAMS) $(man_MANS)

