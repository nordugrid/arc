#ifndef SCHED_RESOURCE
#define SCHED_RESOURCE

#include <string>

namespace Arc {

class Resource {
    private:
        std::string id;
    public:
        Resource(void);
};

}; // namespace Arc

#endif // SCHED_RESOURCE
