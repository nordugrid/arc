#ifndef SCHED_RESOURCE
#define SCHED_RESOURCE

#include <string>

namespace GridScheduler {

class Resource {
    private:
        std::string id;
    public:
        Resource(void);
};

}; // namespace Arc

#endif // SCHED_RESOURCE
