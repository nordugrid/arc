pythondir= $(PYTHON_SITE_LIB)

pkglibexec_SCRIPTS = ssm_master

python_DATA = SecureStompMessenger.py EncryptUtils.py message_db.py ssm.cfg ssm.log.cfg

EXTRA_DIST = $(python_DATA) $(pkglibexec_SCRIPTS)
