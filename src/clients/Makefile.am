if XMLSEC_ENABLED
VOMS_SAML_CLIENT = voms
else
VOMS_SAML_CLIENT =
endif

SUBDIRS = data echo isis credentials arclib charon $(VOMS_SAML_CLIENT) srm jura
DIST_SUBDIRS = data echo isis credentials arclib charon voms srm jura
