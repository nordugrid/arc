if GRIDFTP_ENABLED
ARC0 = ARC0
else
ARC0 =
endif

if PYTHON_SWIG_ENABLED
PYTHONBROKER = PythonBroker
else
PYTHONBROKER =
endif

if EMIES_ENABLED
EMIES = EMIES
else
EMIES =
endif

if ARCREST_ENABLED
ARCREST = ARCREST
else
ARCREST =
endif

if LDNS_ENABLED
ARCHERY = ARCHERY
else
ARCHERY =
endif

SUBDIRS = TEST $(ARC0) $(ARCREST) $(EMIES) Broker $(PYTHONBROKER) JobDescriptionParser $(ARCHERY) ldap
DIST_SUBDIRS = TEST ARC0 ARCREST EMIES Broker PythonBroker JobDescriptionParser ARCHERY ldap
